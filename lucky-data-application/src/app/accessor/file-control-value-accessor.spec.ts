import { FileControlValueAccessor } from './file-control-value-accessor';

describe('FileControlValueAccessor', () => {
  it('should create an instance', () => {
    expect(new FileControlValueAccessor()).toBeTruthy();
  });
});
